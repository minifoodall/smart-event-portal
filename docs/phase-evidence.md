# Phase Evidence (screenshot placeholders)

Drop your screenshots into `docs/screenshots/` with the names below. They're referenced from the README and from the rubric's presentation demo.

| Phase | Recommended file                                  | What to capture                              |
|-------|---------------------------------------------------|----------------------------------------------|
| 1     | `phase1-github.png`                               | GitHub repo page with branches + tags        |
| 2     | `phase2a-docker-ps.png`                           | `docker ps` after v1 launch                  |
| 2     | `phase2b-dark-mode.png`                           | v2 dark mode in the browser                  |
| 2     | `phase2c-search.png`                              | v3 event search results                      |
| 2     | `phase2d-docker-hub.png`                          | Docker Hub repository with pushed tags       |
| 3     | `phase3a-pods.png`                                | `kubectl get pods -n event-portal`           |
| 3     | `phase3b-scale-5.png`                             | After scaling backend to 5 replicas          |
| 3     | `phase3c-rolling-update.png`                      | `kubectl rollout status` during v2 update    |
| 3     | `phase3d-rollback.png`                            | `kubectl rollout undo` + history             |
| 4     | `phase4a-jenkins-pipeline.png`                    | Blue Ocean / classic view of the pipeline    |
| 4     | `phase4b-build-success.png`                       | Green build with all stages                  |
| 4     | `phase4c-auto-rollback.png`                       | Failed build triggering auto-rollback        |
| 5     | `phase5-app-running.png`                          | Final app running in browser                 |
